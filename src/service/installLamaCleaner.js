const fs = require("fs");
const path = require("path");

//下载miniconda

const installLamaCleaner = (mainWindow, step) => {
  switch (step) {
    case 1:
      //下载miniconda
      break;
    default:
  }
};
module.exports = installLamaCleaner;
